<p>
  <span>Title:</span>
  <span><%= @book.title %></span>
</p>

<p>
  <span>Body:</span>
  <span><%= @book.body %></span>
</p>

<%= link_to "Edit", edit_book_path(@book.id) %>
<%= link_to "Back", books_path %>